---
import MainLayout from "@/layouts/main-layout.astro";
import { Image } from "astro:assets";
import { hobbies } from "@/config/about.ts";
import { Button } from "@/components/ui/button";
import { Icon } from "astro-icon/components";
---

<MainLayout title="Portafolio" description="Diseño web creativo y responsivo para potenciar tu presencia digital y atraer más clientes. Impulsa tu negocio con una experiencia de usuario excepcional.">
  <section class="space-y-5">
    <section class="relative container max-w-screen-md mb-28 mt-16 sm:mt-20 lg:mt-28">
      <div class="flex h-full flex-col justify-between">
        <div class="grid gap-8 sm:grid-cols-2">
          <div>
            <h1 class="text-4xl font-heading text-foreground">Portafolio</h1>
            <p class="text-base text-muted-foreground mt-0.5">
              Explora nuestros ultimos proyectos.
            </p>
          </div>
          <div>
            <p class="mb-12 text-base text-muted-foreground">
              Con nuestros servicios, buscamos que te destaques, aumentes tu clientela y crezcas con éxito en el entorno digital.
            </p>
            <div class="flex items-center gap-3">
              <Button className="rounded-xl">
                <Icon name="lucide:mail" class="mr-2 size-4" />
                <a href="mailto:">Hablemos, contactanos hoy mismo</a>
              </Button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="relative z-10 flex flex-col justify-center overflow-hidden lg:overflow-visible pb-16">
      <div class="mx-auto flex w-max -translate-x-[15%] justify-center lg:w-full lg:translate-x-0">
        <div class="relative z-10 mx-auto max-w-4xl px-4 md:max-w-6xl md:px-12 lg:max-w-5xl lg:px-6 xl:max-w-[80rem] xl:px-6 2xl:px-0">
          <div class="grid grid-cols-4 items-center gap-4 md:gap-6">
            {/* First Column */}
            <div class="space-y-4 md:space-y-6">
              <Image crossorigin="anonymous"
                class="h-full w-auto object-cover overflow-hidden rounded-xl"
                src={hobbies[0].image}
                alt={hobbies[0].alt}
                width={800}
                height={1226}
                loading="eager"/>
              <div class="h-52 overflow-hidden rounded-xl">
                <Image crossorigin="anonymous"
                  class="w-full h-full object-cover"
                  src={hobbies[1].image}
                  alt={hobbies[1].alt}
                  width={800}
                  height={1334}
                />
              </div>
            </div>

            {/* Second Column */}
            <div class="space-y-4 md:space-y-6">
              <div class="h-48 overflow-hidden rounded-xl">
                <Image crossorigin="anonymous"
                  class="w-full h-full object-cover"
                  src={hobbies[2].image}
                  alt={hobbies[2].alt}
                  width={800}
                  height={983}
                  loading="eager"
                />
              </div>
              <Image crossorigin="anonymous"
                class="rounded-xl"
                src={hobbies[3].image}
                alt={hobbies[3].alt}
                width={800}
                height={1108}
                loading="eager"
              />
              <div class="relative h-48 overflow-hidden rounded-xl">
                <Image crossorigin="anonymous"
                  class="w-full h-full object-cover"
                  src={hobbies[4].image}
                  alt={hobbies[4].alt}
                  width={1260}
                  height={750}
                />
              </div>
            </div>

            {/* Third Column */}
            <div class="space-y-4 md:space-y-6">
              <div class="h-56 overflow-hidden rounded-xl">
                <Image crossorigin="anonymous"
                  class="w-full h-full object-cover"
                  src={hobbies[5].image}
                  alt={hobbies[5].alt}
                  width={800}
                  height={1334}
                  loading="eager"
                />
              </div>
              <div class="h-80 w-full overflow-hidden rounded-xl xl:h-[28rem]">
                <Image
                  class="w-full h-full object-cover"
                  src={hobbies[6].image}
                  alt={hobbies[6].alt}
                  width={801}
                  height={2477}
                />
              </div>
            </div>

            {/* Fourth Column */}
            <div class="space-y-4 md:space-y-6">
              <div class="overflow-hidden rounded-xl bg-white">
                <Image crossorigin="anonymous"
                  src={hobbies[7].image}
                  alt={hobbies[7].alt}
                  width={800}
                  height={900}
                  loading="eager"
                />
              </div>
              <div class="overflow-hidden rounded-xl bg-white">
                <Image crossorigin="anonymous"
                  src={hobbies[8].image}
                  alt={hobbies[8].alt}
                  width={800}
                  height={900}
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</MainLayout>
